# Architecture Phase — Gate Check

All items must be completed before advancing to Build phase.
Lines with `>>` are machine-verifiable acceptance criteria.

- [x] System design completed with component diagram and responsibilities
  >> `test -s phases/03-architecture/system-design.md` — file exists and is non-empty
- [x] Data model defined with all entities, fields, and relationships
  >> `test -s phases/03-architecture/data-model.md` — file exists and is non-empty
- [x] API design completed with all endpoints/commands documented
  >> `test -s phases/03-architecture/api-design.md` — file exists and is non-empty
- [x] Tech stack decisions documented with rationale
  >> `test -s phases/03-architecture/tech-stack.md` — file exists and is non-empty
- [x] Phased roadmap defined (MVP → V1 → V2) with clear scope per phase
  >> `test -s phases/03-architecture/roadmap.md` — file exists and is non-empty
- [x] All architecture decisions reviewed and approved by product owner
- [x] No unresolved technical feasibility questions
